Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# NumPy - must use a wheel-supported version on Render
numpy==1.26.4

# Use headless OpenCV (smaller, no GUI dependencies)
opencv-python-headless==4.8.1.78

Pillow==10.0.0

# ReportLab for PDF generation
reportlab==3.6.12

# For Python build backend
setuptools>=65
wheel
